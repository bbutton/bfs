#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}



/*
 * breadthfirst ( root )
{
    enqueue ( root );
    while ( ! empty ( queue ) )
    {
next = dequeue ( queue );
for each node directly below next {
 } }
visit ( node );
if ( isa_directory ( node ) )
    enqueue ( node );
}
 */